<%= form_for :session, url: login_path, html: {class: "form container #{@session.errors.empty? ? '' : ' form--invalid' }"} do |f| %>
  <h2>Вход</h2>
<!--  <div class="form__item<%#= @session.errors[:email].empty? ? '' : ' form__item--invalid' %>">-->
    <%= f.label :email, 'E-mail*' %>
    <%= f.text_field :email, placeholder: "Введите e-mail" %>
    <%# unless @session.errors[:email].empty? %>
<!--      <span class="form__error">Введите e-mail</span>-->
    <%# end %>
<!--  </div>-->

<!--  <div class="form__item form__item--last<%#= @session.errors[:password].empty? ? '' : ' form__item--invalid' %>">-->
    <%= f.label :password, 'Пароль*' %>
    <%= f.text_field :password, placeholder: "Введите пароль" %>
    <%# unless @session.errors[:password].empty? %>
<!--      <span class="form__error">Введите пароль</span>-->
    <%# end %>
<!--  </div>-->

  <%= f.submit "Войти", class: "button" %>
<% end %>
